function toLowerCase(string) {
  let lowerString = '';
  for (let i = 0; i <= string.length - 1; i++) {
    let lowerChar = '';
    if (string.charCodeAt(i) <= 90 && string)
    lowerString += string.charCodeAt(i)
  }
}